FIXME
	Partition for 12mb iso far to big (512M)

 * install grub (instead of syslinux)?
 	* left to adapt:
	 	40_*
	 	5*
 * dynamic partition space (including 1st partition): PARTSIZE='max'
 * name partitions (from dd method)
 * create config files for used methods only
 * extracting method:
 	* fix (global) paths
 	* fix menu entries (extracted_images.cfg)
 * use mostly bash features (instead of e.g. =$(sed ...) )
 * hybrid bios/efi modes
 	info e.g.:
 	https://www.slivermetal.org/2016/09/18/how-to-create-an-hybrid-uefi-gpt-bios-gptmbr-boot-usb-disk/